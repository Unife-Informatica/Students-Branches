# C Compiler

Per compilare un file in C si usa:

```sh
gcc [-Wall -Wextra] -o pippo pippo.c
```

- `-Wall` abilita tutti i messaggi di warning
- `-Wextra` abilita warning aggiuntivi
- `-o` specifica il nome del file eseguibile
