# Risposte
*Fonte*: [20190618_t1.pdf](./20190618_t1.pdf)

1. 
    Uno **stack frame** è una porzione dello stack di esecuzione (stack della memoria) utilizzata per gestire l'esecuzione di una singola chiamata di funzione. Ogni volta che viene chiamata una funzione, viene creato un nuovo stack frame che contiene informazioni necessarie per eseguire la funzione e tornare al chiamante.

    Composizione:
    - Valori dei parametri della funzione
    - Variabili locali
    - Puntatore di ritorno

2. N matricola: 205330  --> output: 0023050000

3. Una **struct** è un tipo di dato che permette di raggruppare variabili di tipi diversi. Ogni membro della struct occupa una parte separata della memoria, e quindi i membri della struct possono essere accessibili indipendentemente l'uno dall'altro. Mentre tutti i membri di una **union** condividono lo stesso spazio di memoria. Questo significa che, in qualsiasi momento, una union può contenere solo un membro alla volta.